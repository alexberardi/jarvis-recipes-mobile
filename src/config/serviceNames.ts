/**
 * How these services are named in jarvis-config-service's registry.
 *
 * They match the ids in jarvis-installer / jarvis-admin's service registries,
 * which is what an operator adding the row on the admin Services page will type.
 */
export const AUTH_SERVICE_NAME = 'jarvis-auth';
export const RECIPES_SERVICE_NAME = 'jarvis-recipes-server';
